{
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && mkdir -p dist && cp -r frontend/dist/* dist/",
    "vercel-build": "npm install --prefix frontend && npm run build --prefix frontend && mkdir -p dist && cp -r frontend/dist/* dist/"
  },
  "dependencies": {
    "mysql2": "^3.18.2"
  }
}
