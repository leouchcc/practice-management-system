[build]
builder = "nixpacks"
buildCommand = "cd backend && mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar backend/target/practice-management-system-1.0.0.jar"

[nixpacks]
phases = ["setup", "build", "install", "run"]